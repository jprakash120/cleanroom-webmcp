# OS
.DS_Store
Thumbs.db

# editors
.vscode/
.idea/
*.swp

# node (only if you later add a dev server / tooling)
node_modules/
npm-debug.log*

# local artifacts
cleanroom-investigation.json
*.local

# large / messy datasets are for the file picker, not the repo
data/production_runs_large.csv
data/*_large.csv
*.parquet
