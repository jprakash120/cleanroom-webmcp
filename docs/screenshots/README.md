# Screenshots

Drop demo stills here for the README and the Devpost submission. Suggested shots:

1. The full three-column workspace with the sample data loaded.
2. The disclosure dialog open, showing a sensitive column being blocked.
3. The disclosure log with aggregate (teal), raw (amber), and denied (red) entries.
4. Two hypothesis cards after the agent flips one to *rejected* and one to *supported*.

Then reference them near the top of `../../README.md`, e.g.
`![Cleanroom workspace](docs/screenshots/workspace.png)`.
